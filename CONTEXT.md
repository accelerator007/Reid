# ملف التسليم الشامل — مشروع رِيْد (Reid)
### وثيقة داخلية · محمية من النشر · آخر تحديث: 12 يوليو 2026
> **الغرض**: افتح هذا الملف في أي شات جديد مع Claude ليفهم كل شيء ويكمل من حيث توقفنا.
> **⚠️ أمان**: هذا الملف في `.gitignore` — لا تدفعه أبدًا لـ GitHub (الموقع يخدم جذر المستودع علنًا!).

---

## 1. ما هو المشروع؟

**رِيْد (Reid)** شركة تقنية عمانية ناشئة (غير مسجّلة رسميًا بعد) — تطوير منتجات برمجية واستشارات تقنية وحلول ذكاء اصطناعي/إنترنت أشياء لقطاعي **الطاقة والصحة**.

- **الموقع الحيّ**: https://reidpro.com (عربي RTL أساسي + نسخة إنجليزية `/en/`)
- **المنتجان**:
  - **RATQ (رتق)**: نظام إصلاح ذاتي مستقل لتسريبات الغاز — صيانة تنبؤية + طائرة مسيّرة. *حاليًا في «وضع المسابقات» فقط، بلا تطوير كبير.*
  - **COR (كور)**: محطة فحص تفاعلية للأطفال بشخصية «كابتن كاردو» — **الأولوية التجارية الأولى**.
- **الفريق**: 2–3 خريجون جدد متفرّغون، مهارات تقنية بالكامل (برمجة + عتاد/مدمجة + AI)، **فجوة: لا أحد متخصص بالبيع**.
- **علاقات متاحة**: جامعة + جهة صحية (باب بايلوت COR).
- **البريد الرسمي**: contact.us@reidpro.com (Zoho) · **واتساب**: ‎+968 97308003
- **لينكدإن**: https://www.linkedin.com/company/135245905/ (الحساب الحقيقي الوحيد — لا يوجد X أو GitHub عام)

## 2. الوثائق المرجعية الداخلية (كلها في جذر المشروع، كلها في .gitignore)

| الملف | المحتوى |
|---|---|
| `BUSINESS-PLAN.md` | خطة الأعمال الكاملة: SWOT، استراتيجية «قطار بثلاث عربات»، خارطة 12 شهرًا، تسعير استرشادي، مخاطر |
| `ROADMAP.md` | خطة التحسين المستقبلية للموقع والشركة بأربعة آفاق زمنية + KPIs ربع سنوية |
| `IMPROVEMENT-PLAN.md` | خطة تحسينات الموقع الأولى (أغلبها نُفّذ) |
| `README.md` | توثيق تقني للمشروع وخطوات النشر |
| `CONTEXT.md` | هذا الملف |

**خلاصة الاستراتيجية**: الخدمات تدفع الفواتير الآن (3 عملاء SMEs في 90 يومًا) → COR يبني المستقبل (عرض حي → مذكرة تفاهم بايلوت → دراسة أثر → أول عقد) → المسابقات وبرامج الدعم العمانية وقود (تقديم شهري واحد جاد). RATQ نائم استراتيجيًا للمسابقات فقط. التسجيل الرسمي عند أول عميل لا عند التمويل. أول أولوية مطلقة: **اتفاق مؤسسين مكتوب + سد فجوة البيع**.

## 3. البنية التقنية للموقع

موقع ثابت خام (HTML/CSS/JS بلا أطر ولا خطوة بناء):

```
index.html              الرئيسية عربي (hero mesh متحرك، bento خدمات، marquee،
                        مشاريع full-bleed، شريط إحصائيات داكن، FAQ، نموذج تواصل)
en/index.html           الرئيسية إنجليزي (مطابقة بنيويًا، LTR)
projects/{ratq,cor}.html + en/projects/…   دراسات حالة (4 صفحات)
blog/index.html         فهرس المدونة (عربي فقط)
blog/predictive-maintenance.html   المقال الأول (الصيانة التنبؤية)
privacy.html + en/privacy.html     سياسة الخصوصية
404.html · robots.txt · sitemap.xml · site.webmanifest · _headers
styles.css              كل الأنماط (متغيرات CSS، الهوية البنفسجية المثراة)
fonts.css + assets/fonts/   خطوط مستضافة ذاتيًا (Alexandria + IBM Plex Sans Arabic woff2)
script.js               كل الحركة والوظائف (انظر §4)
assets/img/             favicon.svg (المثلث) · apple-touch-icon · icon-192/512 ·
                        og-image.png (1200×630) · ratq.svg · cor.svg (رسوم متحركة داخليًا)
deploy.sh               نشر بديل عبر wrangler (غير مستخدم حاليًا — النشر عبر git push)
```

**الهوية**: بنفسجي `#55418B` + خزامي `#7D80DA` + نيلي غامق `#2E2352` + ليلك `#A78BFA` + خلفية `#F5F5F4`. خط العناوين Alexandria، النصوص IBM Plex Sans Arabic. **الشعار الحالي مؤقت**: مثلث محدّد بداخله مثلث مصمت (SVG مضمّن في كل الصفحات + favicon).

**ميزات في script.js**: ظهور متتابع + تقسيم كلمات العناوين، mesh متحرك، parallax، إمالة 3D، أزرار مغناطيسية، مؤشر مخصص، شريط تقدم تمرير، scroll-spy، عدّادات، قائمة جوال، نموذج تواصل (FormSubmit AJAX + سقوط لـ mailto)، فتح موثوق لروابط واتساب، **سكافولد حجز Cal.com** (انظر §6). كل الحركة محروسة بـ `prefers-reduced-motion` و`pointer: fine`.

## 4. البنية التحتية والنشر (مهم جدًا — اقرأ بدقة)

- **الاستضافة**: Cloudflare **Worker** اسمه `reid` (وليس مشروع Pages!) يخدم **جذر مستودع GitHub مباشرة**، مربوط بـ:
  - **مستودع GitHub**: `accelerator007/Reid` (فرع `main`) — **كل push إلى main يُنشر تلقائيًا** خلال ~دقيقة.
  - **حساب Cloudflare**: alialajmi524@gmail.com (Account ID: d3ec849fa696c9d337202a6f2ca23fd0) — فيه الدومين reidpro.com أيضًا.
  - **مصادقة git**: عبر osxkeychain على هذا الجهاز (حساب GitHub: accelerator007).
- **⚠️ القاعدة الأمنية الذهبية**: أي ملف يصل origin/main يصبح **علنيًا على reidpro.com** (حدث فعلًا أن انكشفت خطة الأعمال وأصلحناها). لذلك كل الوثائق الداخلية في `.gitignore` محليًا **وعلى origin/main**. لا تستخدم `git add -f` معها أبدًا.
- **⚠️ تاريخ git المحلي متباعد عن origin/main**: المحتوى متطابق لكن الـ commits مختلفة (البعيد فيه commits برسائل مثل "klj", "ddd" من جهة المالك). **آلية النشر المعتمدة (overlay-push)**:
  ```bash
  # 1) commit محلي عادي على main
  # 2) ادفع الملفات المتغيرة فقط فوق origin/main عبر worktree نظيف:
  WT=$(mktemp -d) && git worktree add -q "$WT" origin/main && cd "$WT"
  git checkout <LOCAL_SHA> -- <قائمة الملفات المتغيرة فقط>
  git status --short   # تأكد يدويًا: لا BUSINESS-PLAN ولا ROADMAP ولا CONTEXT...
  git add -A && git commit -m "..." && git push origin HEAD:main
  cd - && git worktree remove --force "$WT"
  ```
- **التحقق بعد كل نشر**: `curl -o /dev/null -w '%{http_code}' https://reidpro.com/<الصفحة>` + تأكد أن الوثائق الداخلية ترجع 404. الـ HTML يتحدث فورًا (max-age=0) لكن كاش CDN قد يتأخر دقائق؛ الأصول `/assets/img/*` مخزنة شهرًا — عند تغييرها بدّل `?v=N` في المراجع.
- **wrangler**: مسجّل دخول بحساب alialajmi524. مشروع Pages باسم reid-web أُنشئ خطأً وحُذف. `deploy.sh` موجود كخيار بديل لكنه غير مستخدم.

## 5. سجل ما أُنجز (بالترتيب)

1. بناء الموقع كاملًا من تصميم Claude Design (ثنائي اللغة + دراسات حالة + SEO + نموذج + واتساب).
2. إعادة تصميم جريئة: هوية مثراة، bento، marquee، حركة كثيفة، رسوم SVG متحركة.
3. إصلاح موثوقية روابط واتساب (فتح مضمون حتى مع حجب النوافذ).
4. ربط لينكدإن الحقيقي.
5. الشعار المؤقت الجديد (المثلث) في كل الأصول + og-image + الأيقونات.
6. خطة الأعمال + خارطة الطريق (وثائق داخلية).
7. **حادثة أمنية معالجة**: انكشاف الوثائق الداخلية علنًا → أزيلت من origin/main وأضيفت لـ .gitignore بالجهتين.
8. سياسة الخصوصية (ع/en) + تنظيف الروابط الميتة + Organization/WebSite schema موسّعة + سكافولد تحليلات Cloudflare.
9. المدونة + أول مقال (الصيانة التنبؤية) + Service/Breadcrumb/Article schema + web manifest + أيقونات + CTA مخصصة بتتبّع + سكافولد حجز Cal.com.

## 6. المعلّق — ينتظر المالك (وكيف يُفعَّل)

| البند | كيف يُفعَّل |
|---|---|
| **حجز Cal.com** | المالك سينشئ الحساب. ثم في `script.js` أعلى الملف: `const BOOKING_URL = 'https://cal.com/...'` — الأزرار المخفية (`data-book`) تظهر تلقائيًا في النسختين |
| **تحليلات Cloudflare** | داشبورد Cloudflare → Analytics → Web Analytics → تفعيل reidpro.com (حقن تلقائي). أو لصق التوكن في التعليق الجاهز في `<head>` الصفحتين. أحداث التتبع جاهزة: nav-cta، hero-consult، form-submit، whatsapp-cta/float، cor-demo-request، ratq-discuss، booking-open |
| **تفعيل FormSubmit** | أول إرسال حقيقي من النموذج يرسل رابط تفعيل لبريد contact.us@reidpro.com — يُضغط مرة واحدة |
| **قسم الشهادات** | جاهز معطّلًا (معلّق HTML) في index.html — يُفعَّل عند توفر شهادات حقيقية بأسماء |
| **حسابات X/GitHub** | أزيلت من التذييل (كانت وهمية) — تُعاد عند إنشاء حسابات حقيقية |
| **الشعار الدائم** | الحالي مؤقت؛ هوية كاملة لاحقًا (بند في ROADMAP) |
| **www.reidpro.com** | لا يوجد سجل DNS لـ www — يُضاف من داشبورد Cloudflare إن رغبوا |

## 7. أفكار الخطوة التالية (من ROADMAP)

- **المقال الثاني**: «كيف تجعل فحص الأطفال تجربة ممتعة؟» — يسوّق COR للجهات الصحية (اقتُرح على المالك).
- صفحات هبوط منتج مخصّصة لـ COR وRATQ بنموذج «اطلب بايلوت».
- Lead magnet (ورقة بيضاء مقابل بريد) + CRM مجاني مربوط بالنموذج.
- تحصين النشر جذريًا: نقل الموقع لمجلد `public/` منفصل أو ضبط الـ Worker ليمنع خدمة `.md`/`.sh` (يتطلب الوصول لإعداد الـ Worker).
- بيئة staging + CMS خفيف لاحقًا.

## 8. أوامر مفيدة للجلسة الجديدة

```bash
cd /Users/ali/Documents/Reid
# معاينة محلية (المنفذ 4173 معرف في .claude/launch.json باسم reid-site):
python3 -m http.server 4173
# حالة النشر مقابل البعيد:
git fetch origin main && git log --oneline origin/main -3
# فحص سريع للموقع الحي:
curl -o /dev/null -w '%{http_code}\n' https://reidpro.com/
# تأكيد حماية الوثائق (يجب 404):
for f in BUSINESS-PLAN.md ROADMAP.md CONTEXT.md; do curl -o /dev/null -w "$f: %{http_code}\n" -sL "https://reidpro.com/$f"; done
```

**ملاحظات سلوكية للمساعد الجديد**: المالك يكتب بالعربية (لهجة خليجية/عمانية) ويفضّل التنفيذ الشامل («سويهم كلهم») مع ملخصات واضحة بالعربية. المعاينة داخل Claude تحجب المواقع الخارجية (روابط واتساب لا تفتح فيها — هذا طبيعي وليس عطلًا). لقطات الشاشة بعد تمرير برمجي غير موثوقة في اللوحة — استخدم فحص DOM بدلها.
