# خطة التحسين الشاملة — موقع رِيْد (Reid)

> الحالة الحالية: صفحة هبوط عربية (RTL) ثابتة من ملفات `index.html` + `styles.css` + `script.js`، منفّذة بالكامل من تصميم Claude Design.
> الهدف: تحويلها من صفحة تعريفية جميلة إلى موقع احترافي يجلب عملاء فعليين.

---

## المرحلة 1 — أساسيات لا يصح الإطلاق بدونها (أسبوع)

### 1.1 التحويل والتواصل (الأولوية القصوى)
- [ ] **نموذج تواصل حقيقي** بدل رابط `mailto:` الحالي — الاعتماد على mailto يفقد معظم الزوار (لا يعمل إن لم يكن لديهم تطبيق بريد مهيأ). الحل: نموذج (اسم، بريد، وصف المشروع) عبر خدمة مثل Formspree أو Web3Forms، أو API خلفي لاحقًا.
- [ ] **زر واتساب مباشر** — الرقم عماني (‎+968 97308003) والجمهور خليجي؛ واتساب هو قناة التواصل الأولى. زر عائم أو ضمن قسم "تواصل معنا".
- [ ] **حجز جلسة التعارف فعليًا** — الـ CTA يعد بـ"جلسة 30 دقيقة"؛ اربطه بـ Calendly/Cal.com بدل البريد.
- [ ] **روابط تواصل اجتماعي حقيقية** — أزرار in / X / gh حاليًا تشير إلى `#contact`. ضع الروابط الفعلية أو احذف ما لا يوجد له حساب، واستبدل النصوص بأيقونات SVG حقيقية مع `aria-label`.

### 1.2 الهوية والأصول
- [ ] **شعار حقيقي** — استبدال مربع حرف "ر" بشعار SVG (يمكن تصميمه بنفس التدرج البنفسجي).
- [ ] **Favicon** + أيقونات apple-touch — حاليًا لا يوجد.
- [ ] **صور المشاريع** — استبدال `RATQ system visual` و`COR station visual` بصور/رندرات حقيقية (WebP/AVIF مع `loading="lazy"` و`alt` وصفي). هذه أكبر فجوة مصداقية في الصفحة.

### 1.3 SEO الأساسي
- [ ] `og:image` (صورة مشاركة 1200×630 بهوية رِيْد) + بطاقات Twitter + `canonical`.
- [ ] بيانات منظمة JSON-LD من نوع `Organization` (الاسم، الشعار، البريد، الهاتف، عُمان).
- [ ] `robots.txt` + `sitemap.xml`.
- [ ] صفحة 404 مخصصة.

### 1.4 النشر
- [ ] تهيئة Git للمشروع (غير مهيأ حاليًا).
- [ ] استضافة ثابتة: Cloudflare Pages أو Netlify أو GitHub Pages، مع ربط نطاق (reidpro.com مستخدم في البريد — اربط الموقع به) وتفعيل HTTPS.

---

## المرحلة 2 — مصداقية وأداء (أسبوعان)

### 2.1 المحتوى والإقناع
- [ ] **قسم شهادات العملاء** — الإحصائيات (+40 مشروعًا، 98% رضا) تحتاج إثباتًا؛ 2–3 اقتباسات بأسماء ومناصب حقيقية، أو شعارات جهات تعامَلتم معها.
- [ ] **صفحة تفصيلية لكل مشروع (Case Study)** — لكل من RATQ وCOR: المشكلة → الحل → النتائج بالأرقام. تتحول بطاقات المشاريع إلى روابط.
- [ ] **مراجعة الأرقام** — تأكد أن +40/+25/98% قابلة للدفاع عنها أمام عميل يسأل.
- [ ] **أسئلة شائعة (FAQ)** — كم تستغرق المشاريع؟ كيف تُسعّرون؟ ماذا يشمل الدعم؟ (تدعم SEO أيضًا عبر `FAQPage` schema).

### 2.2 الأداء
- [ ] **استضافة الخطوط ذاتيًا** (Alexandria + IBM Plex Sans Arabic بصيغة woff2، بالأوزان المستخدمة فقط) — يزيل الاعتماد على Google Fonts ويُحسّن LCP ويحل مسألة الخصوصية (GDPR).
- [ ] `preload` لخط العناوين، والتأكد من `font-display: swap`.
- [ ] **تخفيف `backdrop-filter`** على الجوال — مكلف على الأجهزة الضعيفة؛ استبداله بخلفيات شبه معتمة عبر media query.
- [ ] تصغير CSS/JS عند النشر (خطوة بناء بسيطة أو تصغير يدوي — الحجم صغير حاليًا).
- [ ] فحص Lighthouse بهدف 90+ في المحاور الأربعة، وقياس Core Web Vitals بعد الربط بالنطاق.

### 2.3 إمكانية الوصول (A11y)
- [ ] **فحص تباين الألوان** — النصوص الفاتحة (`#8a7f96` على `#F5F5F4`، ونصوص الفوتر شبه الشفافة) قد تكون دون نسبة 4.5:1؛ تعديل الدرجات عند الحاجة.
- [ ] **حالات تركيز واضحة** — `:focus-visible` بحلقة بنفسجية على الروابط والأزرار (حاليًا الافتراضي فقط).
- [ ] رابط "تخطَّ إلى المحتوى" لمستخدمي لوحة المفاتيح.
- [ ] إغلاق قائمة الجوال بزر Escape، وحبس التركيز داخلها عند الفتح.
- [ ] `aria-label` للأيقونات الاجتماعية بأسماء المنصات الكاملة.

### 2.4 تجربة الاستخدام
- [ ] **Scroll-spy** — تمييز رابط القسم النشط في شريط التنقل أثناء التمرير.
- [ ] تعويض ارتفاع الشريط الثابت عند القفز للأقسام (`scroll-margin-top: 88px` على الأقسام) — حاليًا يغطي الشريطُ بدايةَ القسم.
- [ ] زر "العودة للأعلى" يظهر بعد تمرير طويل.
- [ ] تحديث سنة الحقوق تلقائيًا بـ JS بدل "2026" الثابتة.

---

## المرحلة 3 — نمو وتوسّع (شهر+)

### 3.1 تعدد اللغات
- [ ] **نسخة إنجليزية LTR** — العملاء في قطاعي الطاقة والصحة غالبًا مؤسسات دولية؛ صفحة `/en/` بمبدّل لغة في الشريط + `hreflang`.

### 3.2 القياس والتحسين المستمر
- [ ] تحليلات تحترم الخصوصية (Plausible أو Cloudflare Analytics — بلا كوكيز وأخف من GA4).
- [ ] تتبع أحداث التحويل: نقرات "اطلب استشارة"، إرسال النموذج، نقرات واتساب.
- [ ] Google Search Console + فهرسة.

### 3.3 محتوى مستمر
- [ ] **مدونة/رؤى تقنية** — مقالات عن الصيانة التنبؤية، إنترنت الأشياء في الطاقة، التقنية الصحية؛ أهم رافعة SEO عربية طويلة المدى (المنافسة على هذا المحتوى بالعربية منخفضة).
- [ ] صفحة "انضم إلينا" عند الحاجة للتوظيف.

### 3.4 تحسينات بصرية اختيارية
- [ ] وضع داكن (`prefers-color-scheme`) — البنية بمتغيرات CSS جاهزة له.
- [ ] رسوم توضيحية مخصصة للخدمات بدل الأشكال الهندسية المجردة.
- [ ] حركة خفيفة لأرقام الإحصائيات (عدّاد تصاعدي عند الظهور).

---

## ما هو منجز بالفعل في التنفيذ الحالي ✓
- تنفيذ كامل للتصميم بأقسامه الثمانية بـ HTML/CSS/JS نظيف بلا اعتماديات.
- تجاوب كامل: قائمة همبرغر للجوال، شبكات مرنة، مقاسات `clamp()`.
- حركة الظهور عند التمرير مع احترام `prefers-reduced-motion`.
- أساس SEO: عنوان، وصف، `lang="ar" dir="rtl"`، وسوم OG أولية.
- بنية ألوان بمتغيرات CSS قابلة للتوسعة.

## ترتيب الأولويات المقترح (أعلى أثر ÷ أقل جهد)
1. نموذج تواصل + واتساب (يحوّل الزوار إلى عملاء فورًا)
2. النشر على نطاق reidpro.com
3. صور مشاريع حقيقية + favicon + og:image
4. تباين الألوان وحالات التركيز
5. استضافة الخطوط ذاتيًا
6. شهادات العملاء وصفحات المشاريع
7. النسخة الإنجليزية
